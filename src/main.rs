mod cpu;

// The instruction file is laid out with spacing to easily correlate the
// different addressing modes of each instruction.
#[rustfmt::skip]
mod instr;

fn main() {
    println!("Hello, world!");
}
